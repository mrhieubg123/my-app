import {createModal } from '@reduxjs/toolkit';

const authModal = createModal ({
    name: 'auth',
    initialState: {
        
    },
    reducers: {
       

    },
});
export default authModal.reducer;